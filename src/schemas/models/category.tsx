export class Category {
  constructor(
    id: string,
    name: string,
    color: string,
    isDefault: boolean,     
    isCredit: boolean,     
    isDebit: boolean,     
    order: number,
  ) {}
}